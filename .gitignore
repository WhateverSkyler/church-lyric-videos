# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Secrets
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/

# Generated plates + render scratch (regenerated on demand)
assets/backgrounds/
samples/work/
output/
samples/_smoke*

# Stock footage. The raw downloads stay out - they are large and only ever
# an input to preparation. The PREPARED clips are tracked on purpose: they
# are what actually goes on screen, the church PC has no other way to get
# them, and a background that silently falls back to a flat plate because a
# file is missing is the kind of failure nobody notices until Sunday.
assets/footage/raw/
samples/ref/
work/
dashboard/themes.json

# yt-dlp cookies (optional, machine-specific)
cookies.txt
worker/cookies.txt
